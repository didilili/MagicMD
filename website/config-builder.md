---
title: 配置生成器
description: 在线生成支持自定义输出规则的 .magicmd.toml 配置。
outline: false
aside: false
sidebar: false
---

# 配置生成器

选择发布目标、终端语言、文件命名、媒体路径和可选 GitHub 发布配置，右侧会实时生成 `.magicmd.toml`。你可以快速定制内容包目录、Markdown 文件名、metadata、报告、CLI 输出语言、媒体路径，以及 `magicmd publish github` 复用的内容仓库设置；生成结果只使用 MagicMD 已经支持的字段，可以复制到项目根目录直接使用。

<ClientOnly>
  <ConfigBuilder />
</ClientOnly>
