---
title: Config Builder
description: Generate a copy-ready .magicmd.toml config with custom output rules.
outline: false
aside: false
sidebar: false
---

# Config Builder

Choose a publishing target, terminal language, filenames, and media paths. Quickly customize package folders, Markdown filenames, metadata, reports, CLI output language, and media links. The generated `.magicmd.toml` only uses fields already supported by MagicMD, so you can copy it to your project root and run it directly.

<ClientOnly>
  <ConfigBuilder />
</ClientOnly>
