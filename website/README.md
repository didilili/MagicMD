# MagicMD Website

MagicMD 官网与在线配置生成器，基于 VitePress。

中文是默认语言，英文页面放在 `en/` 目录下。新增或修改中文文档时，记得同步维护对应英文页面。

## Development

```bash
npm install
npm run dev
```

Local URL:

```text
http://127.0.0.1:5173/
```

## Build

```bash
npm run build
```

Static output is written to `dist/` for GitHub Pages.
